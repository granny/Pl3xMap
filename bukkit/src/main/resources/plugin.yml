#file: noinspection YAMLSchemaValidation
name: "${name}"
main: "${group}.Pl3xMapBukkit"
version: "${version}"
api-version: "1.19"
load: "POSTWORLD"
authors: ${authors}
description: "${description}"
website: "${website}"
